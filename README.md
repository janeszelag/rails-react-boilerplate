React & Rails API boilerplate

To install dependencies:
- within root directory: bundle install
- within client directory : npm install

To run development servers:
- within root: bin/rails s
- within client: npm start
